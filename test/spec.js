import 'jsdom-global/register';
import expect from 'expect';
import sinon from 'sinon';

import {
 reactAnnotations
} from '../src';
describe(`react-annotations`, () => {

  it(`works`, () => {
     expect(true).toEqual(true);
  });

})
